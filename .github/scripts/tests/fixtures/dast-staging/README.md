# DAST staging fixtures

These synthetic inputs are intentionally non-routable. They exercise the preflight
validator's exact HTTPS hostname and no-query/no-IP contract without contacting a
staging system.
